const initialState = {
    health: [

    ],
};

export const healthReducer = (state = initialState, action) => {
    // console.log(state, action)
    switch (action.type) {
        default:
            return state
    }
};